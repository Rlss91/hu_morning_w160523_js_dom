window.addEventListener("load", () => {
  //   localStorage.setItem("name", "value to save"); // save data to localStorage
  console.log(localStorage.getItem("name")); // get data from localStorage
});
